[package]
name = "halloc"
version = "0.7.5"
authors = ["фыв"]
edition = "2021"
description = "A custom heap allocator made for use in personal projects"
readme = "README.md"
keywords = ["heap", "allocator", "custom-allocator", "memory", "memory-management"]
publish = false

[dependencies]